<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:paddingLeft="@dimen/activity_horizontal_margin"
                android:paddingRight="@dimen/activity_horizontal_margin"
                android:paddingTop="@dimen/activity_vertical_margin"
                android:paddingBottom="@dimen/activity_vertical_margin"
                tools:context=".ForecastActivity">

    <TextView
            android:id="@+id/lbl_status"
            style="@style/labels"
            android:text="@string/default_status"/>

    <ImageView
            android:id="@+id/img_current_weather"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:minWidth="200dp"
            android:minHeight="200dp"
            android:scaleType="fitXY"
            android:layout_centerHorizontal="true"
            android:contentDescription="@string/hint_current_weather"
            android:layout_below="@id/lbl_status"/>

    <TextView
            android:id="@+id/lbl_current_weather"
            style="@style/labels"
            android:text="@string/default_weather"
            android:layout_below="@id/img_current_weather"/>

</RelativeLayout>
